[package]
name = "cameraman-resize-bench"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
fast_image_resize = { version = "6.0.0", default-features = false, features = ["std", "only_u8x4"] }
